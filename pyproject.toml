[project]
name = "webhook-gateway"
version = "0.1.0"
description = "A robust webhook gateway to ingest, validate, and dispatch webhooks from various sources."
authors = [{ name = "Your Name", email = "you@example.com" }]

requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic",
    "mysqlclient", # Driver for MySQL
    "aiomysql", # Async MySQL driver
    "pymysql", # Required by aiomysql
    "alembic", # For database migrations
    "python-multipart", # For handling form data
    "lxml", # For handling XML
    "pydantic-settings", # For managing settings
    "httpx",
    "aiosqlite>=0.21.0",
    "requests>=2.32.4",
    "chardet>=5.2.0",
    "faststream[rabbit,cli]>=0.5.43",
    "sqlalchemy[asyncio]>=2.0.41",
    "prometheus_client>=0.20.0",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov",
    "black",
    "ruff",
]

[tool.ruff]
line-length = 88

[tool.black]
line-length = 88
